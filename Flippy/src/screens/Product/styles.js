const { StyleSheet } = require("react-native");

const styles = StyleSheet.create({
    image: {
        height: 200,
        width: 200,
        marginLeft: 20,
        marginTop: 100
    },
    heading: {
        fontSize: 30,
        textAlign: 'center',

    }
})
export default styles;